# This invalid config file tests that the validator works
# when no jobs are present.
version: 2.1

jobs:
  job1:
    macos:
    steps:
      - checkout
      - run:
          name: getting the code
          command: |
            ls -al
            echo 'all files'

workflows:
  workflow: